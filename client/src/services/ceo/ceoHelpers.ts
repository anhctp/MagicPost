export enum TransactionTypeWarehouse {
  TRANSACTION = "Transaction offices",
  GATHERING = "Gathering points",
}
export enum TypeWarehouse {
  TRANSACTION = "TRANSACTION",
  GATHERING = "GATHERING",
}
export enum UserRoleLeader {
  LEADERTRANSACTION = "Leader Transaction",
  LEADERGATHERING = "Leader Gathering",
}
